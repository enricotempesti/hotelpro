{% extends '::base.html.twig' %}

{% block body -%}
    <h1>camere</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Numerocamera</th>
                <td>{{ entity.numerocamera }}</td>
            </tr>
            <tr>
                <th>Tipocamera</th>
                <td>{{ entity.tipocamera }}</td>
            </tr>
            <tr>
                <th>Fotocamera</th>
                <td>{{ entity.fotocamera }}</td>
            </tr>
            <tr>
                <th>Fotocamera1</th>
                <td>{{ entity.fotocamera1 }}</td>
            </tr>
            <tr>
                <th>Fotocamera2</th>
                <td>{{ entity.fotocamera2 }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('camere') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('camere_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
